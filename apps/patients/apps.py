from django.apps import AppConfig


class PatientsConfig(AppConfig):
    """Django application configuration for the `patients` app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.patients"
